<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Sign Up | MyApp</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css?family=Montserrat:600,400&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Montserrat', 'Segoe UI', Arial, sans-serif;
            background: linear-gradient(120deg, #e73c7e 0%, #23a6d5 100%);
            background-size: 200% 200%;
            animation: gradientBG 8s ease-in-out infinite;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        @keyframes gradientBG {
            0% {background-position: 0% 50%;}
            50% {background-position: 100% 50%;}
            100% {background-position: 0% 50%;}
        }
        .signup-container {
            background: rgba(255,255,255,0.97);
            padding: 2.5rem 2.5rem 2rem 2.5rem;
            border-radius: 18px;
            width: 100%;
            max-width: 370px;
            box-shadow: 0 8px 32px rgba(35,166,213,0.18), 0 2px 8px rgba(231,60,126,0.10);
            position: relative;
            overflow: hidden;
        }
        .signup-container::before {
            content: '';
            position: absolute;
            top: -60px;
            right: -60px;
            width: 120px;
            height: 120px;
            background: linear-gradient(135deg, #23d5ab 0%, #e73c7e 100%);
            opacity: 0.13;
            border-radius: 50%;
            z-index: 0;
        }
        h1 {
            margin-bottom: 1.3rem;
            font-size: 2rem;
            color: #222;
            text-align: center;
            font-weight: 600;
            letter-spacing: 1px;
        }
        form {
            display: flex;
            flex-direction: column;
            gap: 1.1rem;
            z-index: 1;
            position: relative;
        }
        .input-group {
            position: relative;
        }
        .input-group input {
            width: 100%;
            padding: 0.85rem 2.5rem 0.85rem 1rem;
            border: 1.5px solid #e0e0e0;
            border-radius: 6px;
            font-size: 1rem;
            background: #fafbfc;
            transition: border-color 0.2s, box-shadow 0.2s;
        }
        .input-group input:focus {
            border-color: #23a6d5;
            box-shadow: 0 0 0 2px #23a6d533;
            outline: none;
        }
        .input-group .icon {
            position: absolute;
            right: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: #23a6d5;
            font-size: 1.1rem;
            opacity: 0.7;
        }
        button {
            padding: 0.85rem;
            background: linear-gradient(90deg, #23a6d5 0%, #e73c7e 100%);
            color: #fff;
            border: none;
            border-radius: 6px;
            font-size: 1.08rem;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s, box-shadow 0.2s;
            box-shadow: 0 2px 8px rgba(35,166,213,0.10);
            letter-spacing: 0.5px;
        }
        button:hover {
            background: linear-gradient(90deg, #23d5ab 0%, #ee7752 100%);
            box-shadow: 0 4px 16px rgba(35,166,213,0.13);
        }
        .signin-link {
            display: block;
            margin-top: 1.3rem;
            text-align: center;
            color: #23a6d5;
            text-decoration: none;
            font-size: 1rem;
            font-weight: 500;
            transition: color 0.2s;
        }
        .signin-link:hover {
            color: #e73c7e;
            text-decoration: underline;
        }
        .error-message {
            color: #d32f2f;
            background: #ffeaea;
            border-radius: 4px;
            padding: 0.7rem 1rem;
            margin-bottom: 1rem;
            text-align: center;
            font-size: 1rem;
            border: 1px solid #f8bcbc;
        }
        @media (max-width: 480px) {
            .signup-container {
                padding: 1.5rem 0.7rem 1.2rem 0.7rem;
                max-width: 98vw;
            }
            h1 {
                font-size: 1.3rem;
            }
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
</head>
<body>
    <div class="signup-container">
        <h1>Create Your Account</h1>
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="error-message">
                <%= error %>
            </div>
        <% } %>
        <form action="/signup" method="POST" autocomplete="off">
            <div class="input-group">
                <input type="text" name="username" placeholder="Username" required>
                <span class="icon"><i class="fa fa-user"></i></span>
            </div>
            <div class="input-group">
                <input type="email" name="email" placeholder="Email" required>
                <span class="icon"><i class="fa fa-envelope"></i></span>
            </div>
            <div class="input-group">
                <input type="password" name="password" placeholder="Password" required>
                <span class="icon"><i class="fa fa-lock"></i></span>
            </div>
            <button type="submit">Sign Up</button>
        </form>
        <a class="signin-link" href="/signin">Already have an account? <b>Sign In</b></a>
    </div>
</body>
</html>
