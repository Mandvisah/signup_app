<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome - AuthFlow</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        :root {
            --primary: #6366f1;
            --primary-dark: #4f46e5;
            --secondary: #ec4899;
            --accent: #14b8a6;
            --success: #10b981;
            --bg-dark: #0f0f23;
            --bg-card: rgba(255, 255, 255, 0.05);
            --text-primary: #ffffff;
            --text-secondary: rgba(255, 255, 255, 0.7);
            --shadow-primary: rgba(99, 102, 241, 0.3);
            --shadow-success: rgba(16, 185, 129, 0.3);
        }
        
        body {
            font-family: 'Inter', sans-serif;
            background: var(--bg-dark);
            color: var(--text-primary);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }
        
        /* Animated Background */
        .animated-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: linear-gradient(135deg, #0f0f23, #1a1a3e, #2d1b69);
        }
        
        .floating-orbs {
            position: absolute;
            width: 100%;
            height: 100%;
        }
        
        .orb {
            position: absolute;
            border-radius: 50%;
            filter: blur(2px);
            animation: float 20s infinite linear;
        }
        
        .orb:nth-child(1) {
            width: 300px;
            height: 300px;
            background: radial-gradient(circle, var(--success) 0%, transparent 70%);
            top: -10%;
            left: -10%;
            animation-delay: 0s;
        }
        
        .orb:nth-child(2) {
            width: 250px;
            height: 250px;
            background: radial-gradient(circle, var(--primary) 0%, transparent 70%);
            top: 50%;
            right: -15%;
            animation-delay: -7s;
        }
        
        .orb:nth-child(3) {
            width: 200px;
            height: 200px;
            background: radial-gradient(circle, var(--secondary) 0%, transparent 70%);
            bottom: -5%;
            left: 50%;
            animation-delay: -14s;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px) translateX(0px) rotate(0deg); }
            25% { transform: translateY(-30px) translateX(20px) rotate(90deg); }
            50% { transform: translateY(0px) translateX(40px) rotate(180deg); }
            75% { transform: translateY(30px) translateX(20px) rotate(270deg); }
        }
        
        /* Confetti Animation */
        .confetti {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1000;
        }
        
        .confetti-piece {
            position: absolute;
            width: 10px;
            height: 10px;
            background: var(--primary);
            animation: confetti-fall 3s linear infinite;
        }
        
        .confetti-piece:nth-child(1) { left: 10%; animation-delay: 0s; background: var(--primary); }
        .confetti-piece:nth-child(2) { left: 20%; animation-delay: 0.5s; background: var(--secondary); }
        .confetti-piece:nth-child(3) { left: 30%; animation-delay: 1s; background: var(--success); }
        .confetti-piece:nth-child(4) { left: 40%; animation-delay: 1.5s; background: var(--accent); }
        .confetti-piece:nth-child(5) { left: 50%; animation-delay: 2s; background: var(--primary); }
        .confetti-piece:nth-child(6) { left: 60%; animation-delay: 2.5s; background: var(--secondary); }
        .confetti-piece:nth-child(7) { left: 70%; animation-delay: 0.8s; background: var(--success); }
        .confetti-piece:nth-child(8) { left: 80%; animation-delay: 1.3s; background: var(--accent); }
        .confetti-piece:nth-child(9) { left: 90%; animation-delay: 1.8s; background: var(--primary); }
        
        @keyframes confetti-fall {
            0% { 
                top: -10px; 
                transform: rotateZ(0deg); 
                opacity: 1; 
            }
            100% { 
                top: 100vh; 
                transform: rotateZ(720deg); 
                opacity: 0; 
            }
        }
        
        /* Main Container */
        .success-container {
            text-align: center;
            padding: 2rem;
            animation: slideInUp 1s ease-out;
        }
        
        .success-card {
            background: var(--bg-card);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            padding: 4rem 3rem;
            backdrop-filter: blur(30px);
            box-shadow: 
                0 25px 80px rgba(0, 0, 0, 0.3),
                0 10px 40px var(--shadow-success);
            position: relative;
            overflow: hidden;
            max-width: 600px;
            margin: 0 auto;
        }
        
        .success-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, var(--success), var(--primary), var(--secondary));
            animation: shimmer 3s ease-in-out infinite;
        }
        
        @keyframes shimmer {
            0%, 100% { transform: translateX(-100%); }
            50% { transform: translateX(100%); }
        }
        
        .success-icon {
            width: 120px;
            height: 120px;
            background: linear-gradient(135deg, var(--success), var(--accent));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 2rem;
            font-size: 3rem;
            animation: bounceIn 1s ease-out 0.5s both;
            position: relative;
        }
        
        .success-icon::before {
            content: '';
            position: absolute;
            width: 140px;
            height: 140px;
            border: 3px solid var(--success);
            border-radius: 50%;
            animation: pulse-ring 2s ease-out infinite;
        }
        
        @keyframes bounceIn {
            0% { 
                opacity: 0; 
                transform: scale(0.3); 
            }
            50% { 
                opacity: 1; 
                transform: scale(1.05); 
            }
            70% { 
                transform: scale(0.9); 
            }
            100% { 
                opacity: 1; 
                transform: scale(1); 
            }
        }
        
        @keyframes pulse-ring {
            0% {
                transform: scale(0.33);
                opacity: 1;
            }
            80%, 100% {
                transform: scale(1.5);
                opacity: 0;
            }
        }
        
        .success-title {
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, var(--success), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: fadeInUp 1s ease-out 0.8s both;
        }
        
        .success-message {
            font-size: 1.2rem;
            color: var(--text-secondary);
            line-height: 1.6;
            margin-bottom: 3rem;
            animation: fadeInUp 1s ease-out 1s both;
        }
        
        .success-features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-bottom: 3rem;
        }
        
        .feature-card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 16px;
            padding: 1.5rem;
            transition: all 0.3s ease;
            animation: fadeInUp 1s ease-out both;
        }
        
        .feature-card:nth-child(1) { animation-delay: 1.2s; }
        .feature-card:nth-child(2) { animation-delay: 1.4s; }
        .feature-card:nth-child(3) { animation-delay: 1.6s; }
        
        .feature-card:hover {
            background: rgba(255, 255, 255, 0.08);
            transform: translateY(-5px);
        }
        
        .feature-card-icon {
            width: 50px;
            height: 50px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1rem;
            font-size: 1.2rem;
        }
        
        .feature-card-title {
            font-weight: 600;
            margin-bottom: 0.5rem;
        }
        
        .feature-card-text {
            font-size: 0.9rem;
            color: var(--text-secondary);
        }
        
        .action-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
        }
        
        .btn {
            padding: 1rem 2rem;
            border: none;
            border-radius: 12px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            animation: fadeInUp 1s ease-out 1.8s both;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
        }
        
        .btn-secondary {
            background: var(--bg-card);
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: var(--text-primary);
            backdrop-filter: blur(10px);
        }
        
        .btn:hover {
            transform: translateY(-2px);
        }
        
        .btn-primary:hover {
            box-shadow: 0 15px 40px var(--shadow-primary);
        }
        
        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.1);
        }
        
        @keyframes slideInUp {
            0% { 
                opacity: 0; 
                transform: translateY(60px); 
            }
            100% { 
                opacity: 1; 
                transform: translateY(0); 
            }
        }
        
        @keyframes fadeInUp {
            0% { 
                opacity: 0; 
                transform: translateY(30px); 
            }
            100% { 
                opacity: 1; 
                transform: translateY(0); 
            }
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .success-container {
                padding: 1rem;
            }
            
            .success-card {
                padding: 3rem 2rem;
            }
            
            .success-title {
                font-size: 2rem;
            }
            
            .action-buttons {
                flex-direction: column;
                align-items: center;
            }
            
            .btn {
                width: 100%;
                max-width: 300px;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <!-- Animated Background -->
    <div class="animated-bg">
        <div class="floating-orbs">
            <div class="orb"></div>
            <div class="orb"></div>
            <div class="orb"></div>
        </div>
    </div>
    
    <!-- Confetti -->
    <div class="confetti">
        <div class="confetti-piece"></div>
        <div class="confetti-piece"></div>
        <div class="confetti-piece"></div>
        <div class="confetti-piece"></div>
        <div class="confetti-piece"></div>
        <div class="confetti-piece"></div>
        <div class="confetti-piece"></div>
        <div class="confetti-piece"></div>
        <div class="confetti-piece"></div>
    </div>
    
    <!-- Success Container -->
    <div class="success-container">
        <div class="success-card">
            <div class="success-icon">
                <i class="fas fa-check"></i>
            </div>
            
            <h1 class="success-title">Welcome to AuthFlow!</h1>
            
            <p class="success-message">
                🎉 Congratulations! Your account has been successfully created and you're now signed in. 
                Welcome to our secure and modern authentication platform.
            </p>
            
            <div class="success-features">
                <div class="feature-card">
                    <div class="feature-card-icon">
                        <i class="fas fa-shield-alt"></i>
                    </div>
                    <h3 class="feature-card-title">Secure Account</h3>
                    <p class="feature-card-text">Your data is protected with enterprise-grade security</p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-card-icon">
                        <i class="fas fa-bolt"></i>
                    </div>
                    <h3 class="feature-card-title">Instant Access</h3>
                    <p class="feature-card-text">Lightning-fast authentication and seamless experience</p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-card-icon">
                        <i class="fas fa-users"></i>
                    </div>
                    <h3 class="feature-card-title">Community</h3>
                    <p class="feature-card-text">Join thousands of users in our growing platform</p>
                </div>
            </div>
            
            <div class="action-buttons">
                <a href="/" class="btn btn-primary">
                    <i class="fas fa-home"></i>
                    Go to Dashboard
                </a>
                <a href="/signup" class="btn btn-secondary">
                    <i class="fas fa-user-plus"></i>
                    Invite Friends
                </a>
            </div>
        </div>
    </div>
</body>
</html>
