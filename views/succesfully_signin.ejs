<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Sign In Successful</title>
    <style>
        body {
            background: linear-gradient(-45deg, #23a6d5, #23d5ab, #e73c7e, #ee7752);
            background-size: 400% 400%;
            animation: gradientBG 10s ease infinite;
            min-height: 100vh;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        @keyframes gradientBG {
            0% {background-position: 0% 50%;}
            50% {background-position: 100% 50%;}
            100% {background-position: 0% 50%;}
        }
        .banner {
            margin: 0 auto;
            padding: 48px 56px 36px 56px;
            background: #fff;
            color: #207245;
            border: none;
            border-radius: 22px;
            text-align: center;
            font-size: 1.5rem;
            font-family: 'Segoe UI', Arial, sans-serif;
            max-width: 440px;
            box-shadow: 0 12px 36px rgba(35,166,213,0.15), 0 2px 8px rgba(0,0,0,0.10);
            animation: fadeIn 1.2s;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(30px);}
            to { opacity: 1; transform: translateY(0);}
        }
        .success-icon {
            font-size: 3.5rem;
            color: #23d5ab;
            margin-bottom: 14px;
            display: block;
            animation: pop 0.7s;
        }
        @keyframes pop {
            0% { transform: scale(0.7); opacity: 0; }
            80% { transform: scale(1.1); opacity: 1; }
            100% { transform: scale(1); }
        }
        .banner strong {
            font-size: 2.3rem;
            display: block;
            margin-bottom: 14px;
            color: #23a6d5;
            letter-spacing: 1.2px;
        }
        .username {
            color: #207245;
            font-weight: 700;
            font-size: 1.18rem;
            margin-bottom: 0.5rem;
        }
        .desc {
            color: #444;
            font-size: 1.12rem;
            margin-bottom: 1.7rem;
        }
        button {
            margin-top: 18px;
            padding: 13px 36px;
            font-size: 1.13rem;
            background: linear-gradient(90deg, #23a6d5 0%, #23d5ab 100%);
            color: #fff;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            font-weight: 700;
            box-shadow: 0 2px 8px rgba(35,166,213,0.10);
            transition: background 0.2s, transform 0.2s;
        }
        button:hover {
            background: linear-gradient(90deg, #23d5ab 0%, #23a6d5 100%);
            transform: translateY(-2px) scale(1.04);
        }
        .footer-link {
            display: block;
            margin-top: 2.2rem;
            color: #23a6d5;
            text-decoration: none;
            font-size: 1.03rem;
            font-weight: 500;
            transition: color 0.2s;
        }
        .footer-link:hover {
            color: #e73c7e;
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="banner">
        <span class="success-icon">🎉</span>
        <strong>Sign in successful!</strong>
        <% if (typeof username !== 'undefined' && username) { %>
            <div class="username">Welcome, <%= username %>!</div>
        <% } else { %>
            <div class="username">Welcome to Mandvi's web app.</div>
        <% } %>
        <div class="desc">You have successfully signed in. Explore your dashboard and enjoy all the features!</div>
        <form action="/" method="get">
            <button type="submit">Go to Home</button>
        </form>
        <a class="footer-link" href="/signin">Sign in with another account</a>
    </div>
</body>
</html>