<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Sign In</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css?family=Montserrat:600,400&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Montserrat', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background: linear-gradient(120deg, #e73c7e 0%, #23a6d5 100%);
            background-size: 200% 200%;
            animation: gradientBG 8s ease-in-out infinite;
        }
        @keyframes gradientBG {
            0% {background-position: 0% 50%;}
            50% {background-position: 100% 50%;}
            100% {background-position: 0% 50%;}
        }
        .container {
            background: rgba(255,255,255,0.95);
            padding: 2.5rem 2.8rem 2rem 2.8rem;
            border-radius: 18px;
            width: 100%;
            max-width: 370px;
            box-shadow: 0 8px 32px rgba(35,166,213,0.18), 0 2px 8px rgba(231,60,126,0.10);
            position: relative;
            overflow: hidden;
        }
        .container::before {
            content: '';
            position: absolute;
            top: -60px;
            right: -60px;
            width: 120px;
            height: 120px;
            background: linear-gradient(135deg, #23a6d5 60%, #23d5ab 100%);
            border-radius: 50%;
            opacity: 0.18;
            z-index: 0;
        }
        h1 {
            margin-bottom: 2rem;
            font-size: 2.1rem;
            color: #222;
            text-align: center;
            font-weight: 600;
            letter-spacing: 1px;
        }
        form {
            display: flex;
            flex-direction: column;
            z-index: 1;
            position: relative;
        }
        .input-group {
            position: relative;
            margin-bottom: 1.3rem;
        }
        .input-group input {
            width: 100%;
            padding: 0.85rem 2.5rem 0.85rem 1rem;
            border: 1.5px solid #e0e0e0;
            border-radius: 6px;
            font-size: 1rem;
            background: #f9f9f9;
            transition: border-color 0.2s, box-shadow 0.2s;
            outline: none;
        }
        .input-group input:focus {
            border-color: #23a6d5;
            box-shadow: 0 0 0 2px #23a6d533;
        }
        .input-group .icon {
            position: absolute;
            right: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: #23a6d5;
            font-size: 1.1rem;
            opacity: 0.7;
        }
        button {
            padding: 0.85rem;
            background: linear-gradient(90deg, #e73c7e 0%, #23a6d5 100%);
            color: #fff;
            border: none;
            border-radius: 6px;
            font-size: 1.08rem;
            font-weight: 600;
            cursor: pointer;
            box-shadow: 0 2px 8px rgba(35,166,213,0.10);
            transition: background 0.2s, transform 0.1s;
            margin-top: 0.2rem;
        }
        button:hover {
            background: linear-gradient(90deg, #23a6d5 0%, #e73c7e 100%);
            transform: translateY(-2px) scale(1.03);
        }
        .signup-link {
            display: block;
            margin-top: 1.5rem;
            text-align: center;
            color: #23a6d5;
            text-decoration: none;
            font-size: 1rem;
            font-weight: 500;
            transition: color 0.2s;
        }
        .signup-link:hover {
            color: #e73c7e;
            text-decoration: underline;
        }
        @media (max-width: 480px) {
            .container {
                padding: 1.5rem 0.8rem 1rem 0.8rem;
                max-width: 98vw;
            }
            h1 {
                font-size: 1.4rem;
            }
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
</head>
<body>
    <div class="container">
        <h1>Sign In</h1>
        <form action="/signin" method="POST" autocomplete="off">
            <div class="input-group">
                <input type="text" name="username" placeholder="Username" required>
                <span class="icon"><i class="fa fa-user"></i></span>
            </div>
            <div class="input-group">
                <input type="password" name="password" placeholder="Password" required>
                <span class="icon"><i class="fa fa-lock"></i></span>
            </div>
            <button type="submit">Sign In</button>
        </form>
        <a class="signup-link" href="/signup">Don't have an account? <b>Sign Up</b></a>
    </div>
</body>
</html>
