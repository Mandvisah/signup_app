<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Sign In | Mandvi's Web App</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            background: linear-gradient(-45deg, #23a6d5, #23d5ab, #e73c7e, #ee7752);
            background-size: 400% 400%;
            animation: gradientBG 10s ease infinite;
            margin: 0;
            padding: 0;
            display: flex;
            min-height: 100vh;
            align-items: center;
            justify-content: center;
        }
        @keyframes gradientBG {
            0% {background-position: 0% 50%;}
            50% {background-position: 100% 50%;}
            100% {background-position: 0% 50%;}
        }
        .signin-container {
            background: #fff;
            padding: 2.7rem 2.7rem 2.2rem 2.7rem;
            border-radius: 16px;
            width: 100%;
            max-width: 390px;
            box-shadow: 0 8px 32px rgba(35,166,213,0.13), 0 1.5px 4px rgba(0,0,0,0.10);
            display: flex;
            flex-direction: column;
            align-items: center;
            animation: fadeIn 1.2s;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(30px);}
            to { opacity: 1; transform: translateY(0);}
        }
        .signin-title {
            font-size: 2.2rem;
            font-weight: 700;
            color: #23a6d5;
            margin-bottom: 0.5rem;
            letter-spacing: 1px;
        }
        .signin-desc {
            color: #666;
            font-size: 1.08rem;
            margin-bottom: 1.5rem;
            text-align: center;
        }
        .error-message {
            color: #d32f2f;
            background: #ffeaea;
            border-radius: 4px;
            padding: 0.7rem 1rem;
            margin-bottom: 1rem;
            text-align: center;
            font-size: 1rem;
        }
        form {
            display: flex;
            flex-direction: column;
            gap: 1.1rem;
            width: 100%;
        }
        input[type="text"],
        input[type="password"] {
            padding: 0.9rem;
            border: 1px solid #c9d6e3;
            border-radius: 7px;
            font-size: 1.08rem;
            transition: border-color 0.2s, box-shadow 0.2s;
            background: #f7fafc;
        }
        input[type="text"]:focus,
        input[type="password"]:focus {
            border-color: #23a6d5;
            outline: none;
            box-shadow: 0 0 0 2px #23a6d533;
        }
        button {
            padding: 1rem;
            background: linear-gradient(90deg, #23a6d5 0%, #23d5ab 100%);
            color: #fff;
            border: none;
            border-radius: 8px;
            font-size: 1.15rem;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s, transform 0.2s;
            box-shadow: 0 2px 8px rgba(35,166,213,0.08);
        }
        button:hover {
            background: linear-gradient(90deg, #23d5ab 0%, #23a6d5 100%);
            transform: translateY(-2px) scale(1.03);
        }
        .signup-link {
            display: block;
            margin-top: 1.3rem;
            text-align: center;
            color: #23a6d5;
            text-decoration: none;
            font-size: 1.05rem;
            font-weight: 500;
            transition: color 0.2s;
        }
        .signup-link:hover {
            color: #e73c7e;
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="signin-container">
        <div class="signin-title">Sign In</div>
        <div class="signin-desc">Welcome back! Please sign in to continue.</div>
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="error-message"><%= error %></div>
        <% } %>
        <form action="/signin" method="POST" autocomplete="off">
            <input type="text" name="username" placeholder="Username" required>
            <input type="password" name="password" placeholder="Password" required>
            <button type="submit">Sign In</button>
        </form>
        <a class="signup-link" href="/signup">Don't have an account? Sign Up</a>
    </div>
</body>
</html>
